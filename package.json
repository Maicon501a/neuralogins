{
  "name": "neuralogins",
  "version": "1.0.0",
  "main": "index.js",
  "description": "JavaScript SDK para o Sistema de Login Multi-Tenant",
  "scripts": {
    "test": "echo \"No tests configured\" && exit 0"
  },
  "dependencies": {
    "cross-fetch": "^3.1.5"
  }
}
